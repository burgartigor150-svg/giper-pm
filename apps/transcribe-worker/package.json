{
  "name": "@giper/transcribe-worker",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch --env-file=.env.local src/index.ts",
    "start": "tsx --env-file=.env.local src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'no lint configured'",
    "clean": "rm -rf node_modules dist .turbo"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1044.0",
    "@giper/db": "workspace:*",
    "@giper/integrations": "workspace:*",
    "@giper/shared": "workspace:*",
    "ioredis": "^5.4.1",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  }
}
